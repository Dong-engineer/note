# 前言

前面已经大致了解了什么是依赖。但是还有很多细节并没有记录到位。在这里会再次学习依赖、依赖注入。

但是我们在Bean章节里面学习到，实例化bean还有静态工厂、实例工厂来进行实例化bean。静态工厂、实例工厂的过程成并不是依赖注入，他们的主要目的是对象的创建，但是在对象的创建是可以与依赖注入结合，来达到解耦合的目的。

我会在后面会给出例子，来是实现静态工厂、实例工厂与依赖注入的结合使用。

